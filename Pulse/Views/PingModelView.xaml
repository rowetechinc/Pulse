<UserControl x:Class="RTI.PingModelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Margin="10">
    		<!--Grid.RowDefinitions>
    			<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" /> 		
			</Grid.RowDefinitions-->
			
			<ContentPresenter Content="{Binding PingModelCanvas}" Focusable="true" Grid.Row="0" />
			<ContentPresenter Content="{Binding PingTimingTotalCanvas}" Focusable="true" Grid.Row="1" Margin="25" />
    </StackPanel>
</UserControl>
