<UserControl x:Class="RTI.BaseDataOutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"   
			 xmlns:mahapps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="900">
    <Grid>

        <TabControl>
            <TabItem Header="Data Output">
                <!--Display the Data Output VM-->
                <ContentControl cal:View.Model="{Binding DataOutputVM}" Grid.Row="1"/>
            </TabItem>
            <TabItem Header="WP Dir and Mag">
                <!--Display the WP Mag Dir VM-->
                <ContentControl cal:View.Model="{Binding WpMagDirVM}" Grid.Row="1"/>
            </TabItem>
        </TabControl>





    </Grid>
</UserControl>
